# Ethan Descamps — Mechanical Engineering Portfolio

This repository contains the static public build of Ethan Descamps’s mechanical engineering portfolio.

## GitHub Pages

Publish from the repository root with GitHub Pages. The site uses relative URLs and works when hosted at either a user domain or a repository subpath.

No build step is required. The entry point is `index.html`.

